704. Binary Search
    def search(self, nums, target):
        r = len(nums) - 1
        l = 0
        while r >= l:
            m = (r + l) // 2
            if nums[m] > target:
                r = m - 1
            elif nums[m] < target:
                l = m + 1
            else:
                return m
        
        return -1
Time: O(nlogn)
Explanation: Just plain, classic binary search.
