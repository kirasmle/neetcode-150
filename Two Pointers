125. Valid Palindrome

167. Two Sum II - Input Array is Sorted

15. 3Sum

11. Container with Most Water

42. Trapping Rain Water
    def trap(self, height):
        l = 0
        r = len(height) - 1
        maxl = height[l]
        maxr = height[r]
        rain = 0
        while l < r:
            if maxl < maxr:
                l += 1
                maxl = max(maxl, height[l])
                if maxl - height[l] > 0:
                    rain += maxl - height[l]    
            else:
                r -= 1
                maxr = max(maxr, height[r])
                if maxr - height[r] > 0:
                    rain += maxr - height[r]
        return rain
Time Complexity: O(n)
Explanation: Using two pointers starting at the beginning and end of the array, 
